import { Widget } from "../utils/widgets";
import { Clock } from "./widgets/clock/clock_widget";

export const leftWidgets: Widget[] = [];
export const centerWidgets: Widget[] = [];
export const rightWidgets: Widget[] = [new Clock()];
