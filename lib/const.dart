const double panelHeight = 48;
const double panelWidth = 1366;
const double expandedPanelHeight = panelHeight * 8;

const Duration pollingRate = Duration(milliseconds: 1000);
const Duration toastDuration = Duration(seconds: 3);
